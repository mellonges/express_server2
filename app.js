const express = require('express');const mongoose = require('mongoose');const path = require('path');const cookieParser = require('cookie-parser');const logger = require('morgan');// const PORT =const indexRouter = require('./routes/index');const usersRouter = require('./routes/users');const postUserRouter = require('./routes/postUser');const userDeleteRouter = require('./routes/deleteUser')const app = express();app.use(logger('dev'));app.use(express.json());app.use(express.urlencoded({ extended: false }));app.use(cookieParser());app.use(express.static(path.join(__dirname, 'public')));app.use('/', indexRouter);app.use('/users', usersRouter);app.use("/post", postUserRouter)app.use("/del", userDeleteRouter)try {    mongoose.connect("mongodb+srv://popov:1964197419952001@cluster0.opusj.mongodb.net/DB?retryWrites=true&w=majority", {useUnifiedTopology: true, useNewUrlParser: true})    app.listen(3000, () => {        console.log(`Сервер запущен на 3000 порту`)    });} catch (e) {    console.log(e)}module.exports = app;